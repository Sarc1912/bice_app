"use client"

import React from 'react'

function ChangeIncidence({props}) {

    const handleChange = (props) => {
        
    }

  return (
    <div>
        <button onClick={()=>handleChange(props)}>
            Click
        </button>
    </div>
  )
}

export default ChangeIncidence